<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand mb-0 h1" href="/">Snapask</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="/">Course List<span class="sr-only">(current)</span></a>
      </li>
    </ul>
    <ul class="navbar-nav my-2">
        <% if !current_user %>
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href=<%=new_user_registration_path%>>Sign Up</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href=<%=new_user_session_path%>>Log in</a>
                </li>
            </ul>
        <% else %>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Hi, <%= current_user.email %>
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                  <% if current_user.admin? %>
                    <a class="dropdown-item" href=<%=admin_courses_path%>>Admin panel</a>
                  <% else %>
                    <a class="dropdown-item" href=<%=profiles_path%>>Purchase record</a>
                  <% end %>
                  <div class="dropdown-divider"></div>
                  <a class="dropdown-item" href=<%=destroy_user_session_path%> data-method="delete" >Log out</a>
                </div>
            </li>
        <% end %>
    </ul>
  </div>
</nav>